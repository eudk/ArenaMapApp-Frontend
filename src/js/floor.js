// floor.js
document.addEventListener("DOMContentLoaded", function() {
    // Dette script kan bruges til at tilføje funktionalitet senere
    // F.eks. du kan tilføje en funktion til at håndtere klik events på billederne (hvis nødvendigt).
    console.log("Etage-siden er indlæst.");
});
